# library

The DKB Library itself — all mechanisms in TypeScript. Seven components (Store, Source Model, Convention, Retrieval, Inference, CLI Framework, Error & Exit-Code Taxonomy); see "Technical Specification (Claude's Refactor)" in the Logseq spec. Empty until the v0.2.1 build loop begins.
