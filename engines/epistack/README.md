# epistack

First engine instantiation: the Durable Epistack KB Engine (FLF Epistemic Case Study competition). Stays thin — CLI title, paths, convention seed, `main()`. Spec: `[[Durable Epistack KB Engine]]` in Logseq. Empty until the v0.2.1 build loop begins.
